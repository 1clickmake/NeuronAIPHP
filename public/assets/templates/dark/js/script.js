// Dark Template Script
console.log('Dark template loaded with dynamic asset system!');

$(document).ready(function() {
    // Subtle fade-in for cards (moved from footer.php)
    $('.glass-card').css('opacity', 0).fadeTo(600, 1);
});
